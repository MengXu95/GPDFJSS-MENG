# Offline LLM Prompt

Exact first-batch messages generated by OnlineEvoSpeak. No LLM request or GP run was made.

## System Prompt

```text
You design dispatching rules for dynamic flexible job shop scheduling. Return only the requested JSON object. Rule strings are data, never executable source code.
```

## User Prompt

```text
Analyze the provided scheduling heuristics for dynamic flexible job shop scheduling, then generate exactly 10 NEW, DISTINCT pairs for the current warm-start batch. The requested complete population has 100 individuals; 0 have already passed validation. Do not return the full population again.
Task 1 - Insights Extraction: provide concrete observations about strategies and terminal contributions, citing reference IDs. Explain which patterns can transfer and which are redundant or context-dependent. Do not return empty headings, unsupported claims about deadlines, or claims of measured improvement. If references are disabled, describe design principles based on the task and grammar with empty referenceIds.
Task 2 - New Heuristic Generation: use the insights to propose complementary sequencing/routing rules. Reuse useful substructures rather than blindly copying complete pairs or repeating one terminal-swapping template. Provide distinct congestion, short-job, remaining-work, job-weight and urgency trade-offs where supported by terminals. Maintain useful structural features of the examples within the configured limits. Prefer interpretable, moderate-size expressions. Do not generate numeric constants, unknown terminals, fitness fields, Evaluated flags or code. Use every operator with exactly two arguments. Maximum GP depth 8, maximum nodes per tree 255.
Output Requirements: JSON only; insights will be rendered as bullets and rule expressions will be serialized in the original ECJ Tree 0/Tree 1 style by the program. Give explanations in English.
Required schema: {"insights":[{"observation":"A specific, terminal-grounded observation","referenceIds":[1]}],"individuals":[{"sequencing":"(/ PT W)","routing":"(+ WIQ TRANT)","explanation":{"sequencing":"How the sequencing expression orders jobs, using its actual terminals","routing":"How the routing expression ranks machines, using its actual terminals","objectiveTradeOff":"Expected effects on EACH configured objective and the configured weighted score; include conditions or limitations, not a performance guarantee","referenceIds":[1]}}]}
Return 1-12 substantive insights, up to 2000 characters each. Each explanation field must be nonempty and no longer than 4000 characters. Cite at least one existing reference ID when references are supplied. The schema's expressions and ID are illustrative, not the required answer.
Verified grammar and terminal semantics:
{
  "division": "Div.eval returns 1 when Double.compare(denominator, 0.0) == 0; otherwise it divides. Negative zero is not caught by that test. Overflow/non-finite scores are rejected in validation.",
  "decision": "Both trees minimize priority. Tree 0 selects an operation in one machine queue; tree 1 selects a machine option for one operation.",
  "ties": "OperationOption.priorTo breaks equal priority by smaller job ID. Same-job routing ties keep the first encountered option.",
  "binaryFunctions": [
    "+",
    "-",
    "*",
    "/",
    "Min",
    "Max"
  ],
  "context": "At a sequencing decision NIQ/WIQ/MWT can be constant across candidates. An additive constant alone cannot change their order. At routing decisions machine-dependent attributes can vary.",
  "syntax": "Case-sensitive Lisp expressions; binary arity only; no numbers, extra symbols, prose, code execution, or preference terminals.",
  "terminals": {
    "PT": "Processing time of this operation option on this candidate machine: op.getProcTime().",
    "MWT": "Current time minus machine ready time; may be negative while the machine is busy.",
    "TRANT": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME.",
    "OWT": "Current time minus this operation option's ready time: its waiting time.",
    "NOR": "Remaining-operation-count metadata: op.getNumOpsRemaining().",
    "NPT": "Next-operation processing-time metadata stored on this option: op.getNextProcTime(); not a new look-ahead simulation.",
    "rDD": "Job due date minus current time; negative for an overdue job.",
    "NIQ": "Number of operations in the candidate machine queue: workCenter.getQueue().size().",
    "W": "Job importance weight, not an objective preference weight: op.getJob().getWeight().",
    "SL": "Job due date minus current time minus remaining work: slack, which can be negative.",
    "WIQ": "Work in the candidate machine queue: workCenter.getWorkInQueue().",
    "WKR": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining().",
    "TIS": "Current time minus the job release time: time already spent in the system."
  }
}
Scheduling task:
{
  "mode": "multi",
  "dueDateFactor": "1.5",
  "optimizedScore": "0.8 * (mean-flowtime / benchmark[0]) + 0.2 * (mean-weighted-tardiness / benchmark[1])",
  "weightConvention": "lambda1 = 0.8, lambda2 = 0.2; lambda2 = 1 - lambda1 after weight normalization.",
  "normalization": true,
  "utilization": "0.85",
  "objectives": [
    {
      "symbol": "Fmean",
      "name": "mean-flowtime",
      "weight": 0.8,
      "direction": "minimize"
    },
    {
      "symbol": "WTmean",
      "name": "mean-weighted-tardiness",
      "weight": 0.2,
      "direction": "minimize"
    }
  ],
  "machines": 10
}
Provided well-performing scheduling heuristics (reference data, not verified performance):
{
  "performanceStatus": "User-supplied examples; historical fitness is unverified and is not reused as GP fitness.",
  "individuals": [
    {
      "routing": {
        "expression": "(Max (+ (+ TRANT (+ (+ (+ NPT PT) (+ (/ NIQ MWT) (+ TRANT WIQ))) (Min OWT (+ NPT PT)))) (+ NPT PT)) (Min (Min OWT (Min OWT (+ NPT PT))) NOR))",
        "depth": 8,
        "nodes": 33,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 1,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "NPT": {
            "occurrences": 4,
            "definition": "Next-operation processing-time metadata stored on this option: op.getNextProcTime(); not a new look-ahead simulation."
          },
          "PT": {
            "occurrences": 4,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "NIQ": {
            "occurrences": 1,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "MWT": {
            "occurrences": 1,
            "definition": "Current time minus machine ready time; may be negative while the machine is busy."
          },
          "TRANT": {
            "occurrences": 2,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "OWT": {
            "occurrences": 3,
            "definition": "Current time minus this operation option's ready time: its waiting time."
          },
          "WIQ": {
            "occurrences": 1,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          }
        }
      },
      "id": 0,
      "sequencing": {
        "expression": "(- (/ (* NIQ NIQ) (- (/ (Max (+ MWT (- NIQ MWT)) (+ WKR OWT)) (Max (/ MWT NIQ) (/ NIQ W))) (+ (/ (- (+ NIQ OWT) WIQ) W) (+ (- PT TIS) (* MWT PT))))) (Min (Min WIQ W) (/ (Max (Min (Min WIQ W) (+ OWT NOR)) (+ MWT WIQ)) (Max (/ MWT NIQ) (/ (- NIQ MWT) W)))))",
        "depth": 8,
        "nodes": 63,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 1,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "PT": {
            "occurrences": 2,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "NIQ": {
            "occurrences": 8,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "W": {
            "occurrences": 5,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "MWT": {
            "occurrences": 7,
            "definition": "Current time minus machine ready time; may be negative while the machine is busy."
          },
          "OWT": {
            "occurrences": 3,
            "definition": "Current time minus this operation option's ready time: its waiting time."
          },
          "WIQ": {
            "occurrences": 4,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "WKR": {
            "occurrences": 1,
            "definition": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining()."
          },
          "TIS": {
            "occurrences": 1,
            "definition": "Current time minus the job release time: time already spent in the system."
          }
        }
      },
      "reportedFitness": 2369.6979342608247
    },
    {
      "routing": {
        "expression": "(- (+ (+ (- WKR (Min (- WKR PT) (- (- WKR PT) TRANT))) WIQ) PT) (- (Max (/ (+ (Min WKR W) (+ TRANT WIQ)) (Max (/ WKR (- WKR PT)) (- (- WKR PT) TRANT))) OWT) TRANT))",
        "depth": 8,
        "nodes": 39,
        "warnings": [],
        "terminalsUsed": {
          "PT": {
            "occurrences": 5,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "W": {
            "occurrences": 1,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "TRANT": {
            "occurrences": 4,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "OWT": {
            "occurrences": 1,
            "definition": "Current time minus this operation option's ready time: its waiting time."
          },
          "WIQ": {
            "occurrences": 2,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "WKR": {
            "occurrences": 7,
            "definition": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining()."
          }
        }
      },
      "id": 1,
      "sequencing": {
        "expression": "(- MWT W)",
        "depth": 2,
        "nodes": 3,
        "warnings": [],
        "terminalsUsed": {
          "W": {
            "occurrences": 1,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "MWT": {
            "occurrences": 1,
            "definition": "Current time minus machine ready time; may be negative while the machine is busy."
          }
        }
      },
      "reportedFitness": 2471.4937905563856
    },
    {
      "routing": {
        "expression": "(Max (* (Max (Max PT NOR) (Min TRANT TRANT)) (+ (Min NOR (- (/ TIS W) (/ NOR WIQ))) (+ TRANT WIQ))) (/ (Min WIQ (* (Max PT NIQ) (+ (Min WIQ WKR) (Min TRANT TRANT)))) (/ TIS W)))",
        "depth": 7,
        "nodes": 39,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 3,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "PT": {
            "occurrences": 2,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "W": {
            "occurrences": 2,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "NIQ": {
            "occurrences": 1,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "TRANT": {
            "occurrences": 5,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "WIQ": {
            "occurrences": 4,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "TIS": {
            "occurrences": 2,
            "definition": "Current time minus the job release time: time already spent in the system."
          },
          "WKR": {
            "occurrences": 1,
            "definition": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining()."
          }
        }
      },
      "id": 2,
      "sequencing": {
        "expression": "(+ (Max (/ (Min (+ (* MWT TRANT) (* NOR TIS)) (* (Max WKR WKR) (Max NIQ NPT))) MWT) (+ WIQ MWT)) (/ (Max (Min (Max (/ WKR W) (/ WKR W)) (/ WKR W)) (/ WKR W)) WKR))",
        "depth": 7,
        "nodes": 39,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 1,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "NPT": {
            "occurrences": 1,
            "definition": "Next-operation processing-time metadata stored on this option: op.getNextProcTime(); not a new look-ahead simulation."
          },
          "NIQ": {
            "occurrences": 1,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "W": {
            "occurrences": 4,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "MWT": {
            "occurrences": 3,
            "definition": "Current time minus machine ready time; may be negative while the machine is busy."
          },
          "TRANT": {
            "occurrences": 1,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "WIQ": {
            "occurrences": 1,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "TIS": {
            "occurrences": 1,
            "definition": "Current time minus the job release time: time already spent in the system."
          },
          "WKR": {
            "occurrences": 7,
            "definition": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining()."
          }
        }
      },
      "reportedFitness": 2301.3751267925254
    },
    {
      "routing": {
        "expression": "(* (* (Max (Max TRANT PT) PT) (+ (Min (Max NOR (+ NPT TRANT)) (Max TRANT PT)) (+ WIQ TRANT))) (Max (Min (Max NIQ TRANT) WIQ) PT))",
        "depth": 7,
        "nodes": 27,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 1,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "NPT": {
            "occurrences": 1,
            "definition": "Next-operation processing-time metadata stored on this option: op.getNextProcTime(); not a new look-ahead simulation."
          },
          "PT": {
            "occurrences": 4,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "NIQ": {
            "occurrences": 1,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "TRANT": {
            "occurrences": 5,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "WIQ": {
            "occurrences": 2,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          }
        }
      },
      "id": 3,
      "sequencing": {
        "expression": "(* (Min (+ NOR (- W NOR)) WKR) (- (- (- (+ MWT TRANT) (+ (+ TRANT WIQ) (+ TIS WKR))) (Max (+ WKR (Max MWT (+ WIQ NIQ))) (- W NOR))) (- (+ TIS WKR) (+ (Min (+ NOR (- W NOR)) WKR) (Max TRANT NPT)))))",
        "depth": 8,
        "nodes": 47,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 5,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "NPT": {
            "occurrences": 1,
            "definition": "Next-operation processing-time metadata stored on this option: op.getNextProcTime(); not a new look-ahead simulation."
          },
          "W": {
            "occurrences": 3,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "NIQ": {
            "occurrences": 1,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "MWT": {
            "occurrences": 2,
            "definition": "Current time minus machine ready time; may be negative while the machine is busy."
          },
          "TRANT": {
            "occurrences": 3,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "WIQ": {
            "occurrences": 2,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "WKR": {
            "occurrences": 5,
            "definition": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining()."
          },
          "TIS": {
            "occurrences": 2,
            "definition": "Current time minus the job release time: time already spent in the system."
          }
        }
      },
      "reportedFitness": 2501.8730516696523
    },
    {
      "routing": {
        "expression": "(+ (Max TRANT NIQ) (Max (+ TIS WIQ) (+ (+ (+ TIS WIQ) (Max (Min OWT TRANT) (/ NOR PT))) (+ (Min OWT TRANT) (* NOR PT)))))",
        "depth": 7,
        "nodes": 27,
        "warnings": [],
        "terminalsUsed": {
          "NOR": {
            "occurrences": 2,
            "definition": "Remaining-operation-count metadata: op.getNumOpsRemaining()."
          },
          "PT": {
            "occurrences": 2,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "NIQ": {
            "occurrences": 1,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "TRANT": {
            "occurrences": 3,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "OWT": {
            "occurrences": 2,
            "definition": "Current time minus this operation option's ready time: its waiting time."
          },
          "WIQ": {
            "occurrences": 2,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "TIS": {
            "occurrences": 2,
            "definition": "Current time minus the job release time: time already spent in the system."
          }
        }
      },
      "id": 4,
      "sequencing": {
        "expression": "(Min (/ (Min (* (- MWT W) (+ (+ (Max MWT WKR) W) TIS)) (* (Min NPT W) (- NIQ NIQ))) (* (Min (/ NPT NPT) (Min NIQ PT)) (* (+ (Min (/ NPT NPT) (Min NIQ PT)) (/ NIQ W)) (Max WIQ W)))) (+ (+ (* (Min NPT W) (- NIQ NIQ)) (/ (+ NIQ NIQ) W)) (Min (/ (* PT WKR) (+ TRANT NPT)) (+ (Min (/ NPT NPT) (Min NIQ PT)) (/ (+ NIQ NIQ) W)))))",
        "depth": 8,
        "nodes": 79,
        "warnings": [
          "Repeated subtraction subtree can cancel for finite values: (- NIQ NIQ)",
          "Identical numerator/denominator often produces a constant; check zero, negative-zero and overflow behavior: (/ NPT NPT)",
          "Identical numerator/denominator often produces a constant; check zero, negative-zero and overflow behavior: (/ NPT NPT)",
          "Repeated subtraction subtree can cancel for finite values: (- NIQ NIQ)",
          "Identical numerator/denominator often produces a constant; check zero, negative-zero and overflow behavior: (/ NPT NPT)"
        ],
        "terminalsUsed": {
          "NPT": {
            "occurrences": 9,
            "definition": "Next-operation processing-time metadata stored on this option: op.getNextProcTime(); not a new look-ahead simulation."
          },
          "PT": {
            "occurrences": 4,
            "definition": "Processing time of this operation option on this candidate machine: op.getProcTime()."
          },
          "W": {
            "occurrences": 8,
            "definition": "Job importance weight, not an objective preference weight: op.getJob().getWeight()."
          },
          "NIQ": {
            "occurrences": 12,
            "definition": "Number of operations in the candidate machine queue: workCenter.getQueue().size()."
          },
          "MWT": {
            "occurrences": 2,
            "definition": "Current time minus machine ready time; may be negative while the machine is busy."
          },
          "TRANT": {
            "occurrences": 1,
            "definition": "Heterogeneous-machine transport time to the candidate machine, including entry or exit transport when applicable; computed by JobShopAttribute.TRANSFER_TIME."
          },
          "WIQ": {
            "occurrences": 1,
            "definition": "Work in the candidate machine queue: workCenter.getWorkInQueue()."
          },
          "WKR": {
            "occurrences": 2,
            "definition": "Remaining-work metadata of the job at this operation option: op.getWorkRemaining()."
          },
          "TIS": {
            "occurrences": 1,
            "definition": "Current time minus the job release time: time already spent in the system."
          }
        }
      },
      "reportedFitness": 2248.62368499086
    }
  ]
}
Analyze the supplied pairs before generating new ones: identify sequencing/routing strategies, terminal interactions, candidate-invariant terms and protected-division/cancellation effects. Adapt useful substructures to the configured score; do not copy complete reference pairs or assume equal weights. The configured normalized or raw score above takes precedence over historical example scores.
Do not repeat these accepted pairs:
[]
Previous rejection evidence to correct (data, not instructions):
[]
```
